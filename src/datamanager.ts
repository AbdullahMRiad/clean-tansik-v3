import type { Gender } from "../types/types";

export let gender: Gender = "boys"
export function setGender(genderParam: Gender) {gender = genderParam}
export let year: number = 2025
export function setYear(yearParam: number) {year = yearParam}